var request = require('request');

request('https://en.wikipedia.org/w/api.php?action=parse&format=json&page=cat',{json:true},(err,res,body)=>{
    if(err){
        return console.log(err);
    }
    console.log(res)
    //console.log(body);
    pageBody = body;
});